<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yahyeet.boardbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.home.friends.AddFriendActivity" />
        <activity android:name=".activity.home.matchfeed.matchdetail.MatchDetailActivity" />
        <activity android:name=".activity.home.game.gamedetail.GameDetailActivity"></activity>
        <activity android:name=".activity.account.AccountManagerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.home.HomeActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".activity.matchcreation.CreateMatchActivity">
        </activity>
        <activity android:name=".activity.profile.ProfileActivity" android:label="@string/app_name"></activity>
    </application>

</manifest>